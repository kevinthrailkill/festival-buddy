# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Festival Buddy' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Festival Buddy
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON'

end

target 'Festival Buddy WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Festival Buddy WatchKit App

end

target 'Festival Buddy WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Festival Buddy WatchKit Extension
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON'

end
